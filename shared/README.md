# Shared Styles

Base styles and Tailwind config shared by job-harvester and job-matcher.

- **styles.css** – Base CSS (body, dark mode, #root). Imported by each app after Tailwind.
- **tailwind.config.js** – Base Tailwind config (darkMode, theme). Extended by each app with its own `content` paths.
